# 1. PYTHON ENVIRONMENT AND DEPENDENCIES
/env
/venv
__pycache__/

# 2. KERAS/TENSORFLOW FILES
# The actual saved model (it can be large and should be generated by the user)
best_social_media_model.h5

# 3. KERAS TUNER ARTIFACTS
# The entire folder created by the Keras Tuner search process
keras_tuner_dir/

# 4. DATA AND SCALING ARTIFACTS
# If you ever save the scaled numerical data for reuse (optional, but good practice)
scaled_data.npy
scaler.pkl

# 5. OS and Editor Files (General safety)
.DS_Store
*.swp